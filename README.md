# n8n-ngrok-docker-setup
Quick setup for n8n using Docker Compose with ngrok integration.

# n8n + ngrok + Docker Setup

This repository contains only the `docker-compose.yml` file used in my video tutorial for quickly setting up n8n with Docker and exposing it via ngrok.

## Requirements

Before starting, make sure you have:

- Docker Desktop installed
- Docker Compose enabled
- ngrok account and auth token

## Setup

### 1. Download the file

Clone this repository:

```bash
git clone https://github.com/yourusername/repository-name.git

cd repository-name
